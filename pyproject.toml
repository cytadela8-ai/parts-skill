[project]
name = "find-tme-parts"
version = "0.1.0"
description = "TME part sourcing and BOM review tools"
requires-python = ">=3.13"
dependencies = ["flask==3.1.2"]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ty.rules]
unresolved-import = "error"
